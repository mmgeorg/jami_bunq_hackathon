{
  "name": "items-lambda",
  "version": "1.0.0",
  "description": "Lambda function to handle items API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p dist && zip -r dist/lambda-function.zip index.js goal-finantial-prediction.js openai.js node_modules",
    "build2": "mkdir -p dist && zip -r dist/socketHandler.zip index.mjs node_modules",
    "deploy": "cd ../infra/terraform && terraform apply"
  },
  "dependencies": {
    "@aws-sdk/client-apigatewaymanagementapi": "^3.799.0",
    "@aws-sdk/client-dynamodb": "^3.456.0",
    "@aws-sdk/lib-dynamodb": "^3.456.0",
    "axios": "^1.9.0",
    "csv-parser": "^3.2.0",
    "csv-writer": "^1.6.0",
    "dayjs": "^1.11.13",
    "dotenv": "^16.5.0",
    "json2csv": "^6.0.0-alpha.2",
    "lambda-api": "^0.12.0",
    "modules": "^0.4.0",
    "natural": "^8.0.1",
    "openai": "^4.96.2",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "aws-sdk": "^2.1692.0"
  }
}
