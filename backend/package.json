{
  "name": "items-lambda",
  "version": "1.0.0",
  "description": "Lambda function to handle items API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p dist && zip -r dist/lambda-function.zip index.js node_modules",
    "deploy": "cd ../infra/terraform && terraform apply"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.456.0",
    "@aws-sdk/lib-dynamodb": "^3.456.0",
    "lambda-api": "^0.12.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "aws-sdk": "^2.1506.0"
  }
}
